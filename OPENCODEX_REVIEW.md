# Opencodex 연동 검토 및 UI 개선

기준: coseung2/opencodex `f6735fd552b04ad7ef59abbd99aa54a707121113` (2.8.0-cs.32), 2026-09-21.

## 반영 완료

- 사용량 `range=all-time`은 서버에서 30일로 처리되므로 `all`로 수정. 전체·7일·30일 선택 제공.
- HTTP 200이어도 `error: read_failed`면 정상 0건으로 표시하지 않고 오류 처리. 비용 미산정·계측 누락·기록 잘림 표시.
- Rust에서 인증 토큰을 읽고 curl 표준입력으로 전달. 웹뷰에 토큰을 반환하는 커맨드 등록 제거. 환경 변수와 OCX 홈·포트 설정 지원.
- Windows API로 PC CPU·물리 메모리, OCX 프로세스 CPU·메모리 조회. 첫 CPU 표본과 프로세스 연결 실패는 미확인으로 표시.
- 하단 제공자별 남은 한도와 짧은 막대. 마우스·키보드 포커스로 상세, 리셋 시간, 갱신 시각 표시. 만료된 표본은 한도가 복구됐다고 가정하지 않음.
- 제공자 한도와 CLI 가용성을 분리. 오류 때 이전 값 표시, 미제공 한도는 `—` 표시.
- 접힌 창 380×188, 상세 표시 시 380×440, 펼친 창 520×740. 개요·대화·설정 탭과 창 닫기 추가.
- 동작하지 않는 OAuth·AFK·자동 전환 UI 제거. 실제 Opencodex 대시보드와 CLI 로그인으로 연결.
- 현재 대화의 쓰기 설정을 정확히 표시하고 새 대화에만 변경 적용. 한글 조합 중 Enter 전송 방지.

## 남은 수정 후보 — 실행 수명 관리 우선

1. **이전 실행 이벤트가 새 실행 상태를 덮어쓸 수 있음.** App 이벤트 처리는 활성 run_id 일치 여부를 확인해야 함. 완료 이벤트와 실제 프로세스 종료 사이 재실행 시나리오에서 재현됨.
2. **취소된 실행의 승인 카드가 남음.** 승인 결과 매칭에 run_id를 포함하고 종료·취소 시 미결 카드를 종료 상태로 바꿀 필요. 동일 request_id 재사용 시 이전 카드까지 갱신되는 경우 재현됨.
3. **프로세스 종료 관리.** runner의 stdout EOF 이후 child.wait 구간에도 취소를 전달하고 자식 프로세스 트리 종료·앱 종료 정리를 구현할 필요.
4. **동일 프로젝트 동시 실행.** 별도 대화가 같은 디렉터리를 수정할 수 있어 프로젝트별 실행 잠금 또는 명시적 동시 실행 정책 필요.
5. **권한 명칭.** Antigravity의 쓰기 허용은 `--dangerously-skip-permissions`, OpenCode는 `--auto`로 확장됨. 단순 파일 쓰기 권한보다 넓으므로 어댑터별 동작과 UI 설명을 일치시켜야 함.

## 남은 수정 후보 — 한도·운영

1. Codex 한도 조회는 id=3 계정 응답을 종료 조건으로 사용하므로 id=2 한도 응답이 늦게 오면 누락될 수 있음. 두 응답을 모두 기다리는 방식 필요.
2. 만료된 공식 한도 표본을 availability 재검사 과정에서도 제거해야 함. 이전 100% 표본으로 다시 cooldown에 진입할 가능성은 코드 검토 결과이며 별도 재현 테스트 필요.
3. runtime-port.json이 오래된 경우의 PID·포트 유효성 검증과 config 포트로의 복구 필요.
4. 채팅·실행 맵 크기 제한과 세션 복원은 별도 구현 필요. 서버의 캐시 한도는 즉시 실시간 잔여량과 차이 날 수 있음.

## 검증

- `npm run build`: 통과.
- `cargo test --manifest-path src-tauri/Cargo.toml`: 리소스 조회 포함 기존 54개 통과, 외부 실행 테스트 2개 제외. 추가 오류·메타데이터 테스트 별도 실행.
- `node scripts/check-quota.cjs`: 사용률→잔여량, 0/100%, 미제공, 만료, 초/밀리초 리셋, 크레딧, 모델별 윈도우 검증.
- 실제 Tauri WebView에서 CPU/RAM/OCX 메모리, 사용량, 제공자 한도 확인. 마우스 진입 440px·이탈 188px, 펼침 740px 확인. 개요·대화·설정 가로 넘침 없음.
- 서버 중단·복구 전체 시나리오와 유료 AI 대화 실행은 이번 검증에 포함하지 않음.
